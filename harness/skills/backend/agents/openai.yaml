interface:
  display_name: "Backend Development"
  short_description: "Build and maintain the reusable Python backend template"
  default_prompt: "Use the backend template and its service boundaries for this task."
