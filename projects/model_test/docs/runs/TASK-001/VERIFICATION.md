# TASK-001 · VERIFICATION — 모델 테스트 콘솔

검증일: 2026-09-07 (**소급 정리**. 검증은 구현과 같은 세션에서 수행되어 독립적이지 않다 — 한계로 남긴다)

## 인수 조건 결과

| # | 조건 | 결과 | 근거 |
| --- | --- | --- | --- |
| AC1 | 모델 목록·선택 | 통과 | `/api/v1/models`가 등록부 5종을 반환, 화면 제공자/모델 선택 동작 |
| AC2 | 전환 시 이전 서빙 중단 | 통과 | 전환 후 상태·로그 확인, 단위 테스트 `test_selecting_another_model_replaces_the_served_one` |
| AC3 | 서빙 모델에 맞는 파라미터 노출 | 통과 | 추론 모델에만 `reasoning_effort` 노출, 비추론 모델에서는 숨김(API·UI 확인) |
| AC4 | 프롬프트 결과 스트리밍 표시 | 통과 | 실제 Chrome(CDP) 구동으로 `qwen3.8-27b` 응답 스트리밍 확인 |
| AC5 | 결과 조회·비교 | 통과 | 기록 조회 화면 확인, `POST /runs/compare`가 파라미터 차이만 추려 반환 |
| AC6 | 테스트 통과 | 통과 | 백엔드 37건, 프론트 13건 |

## 실제 모델 검증

| 항목 | 값 |
| --- | --- |
| 모델 | `qwen3.8-27b` (vLLM 0.28.0, B200 1장, GPU 163GB 사용) |
| 서빙 준비 | 185초 |
| 생성(사고 끄기) | 2,089ms / 100 토큰 |
| 생성(사고 켜기) | 3,215ms / 292 토큰(그중 reasoning 171) |
| 사고 분리 | reasoning과 본문이 분리되어 각각 표시됨 |

## 검증 방법

- 단위·통합 테스트: 실제 HTTP 서버를 띄워 SSE까지 왕복(`tests/test_api.py`), 종료 신호 처리(`tests/test_shutdown.py`)
- 브라우저: 가상 시간을 쓰지 않는 실제 Chrome을 CDP로 구동해 입력→스트리밍→완료 확인
- 수동: 원격 접속(0.0.0.0 바인딩), 로그 조회, 서빙 중단/재시작

## 한계

- 검증자가 구현자와 동일하다(하니스 규약의 독립 검증 미충족).
- `gemma-4-31b-it`, `exaone-4.5-33b`, `thinkingcap-qwen3.6-27b`는 실제 서빙까지 확인하지 않았다.
- 부하·동시 요청, 장시간 안정성은 검증 범위 밖이다.
