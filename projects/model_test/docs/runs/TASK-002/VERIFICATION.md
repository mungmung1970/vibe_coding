# TASK-002 · VERIFICATION

검증일: 2026-09-07
대상: FEAT-002 사고 과정 진행 가시화
환경: `qwen3.8-27b` 서빙 중(vLLM 0.28.0, B200 1장), 실제 Chrome을 CDP로 실시간 구동(가상 시간 미사용)

## 인수 조건 결과

| # | 조건 | 결과 | 근거 |
| --- | --- | --- | --- |
| AC1 | 사고 중 진행 표시가 2초 이내 노출 | **통과** | 최초 표시 **57ms**, 문구 `사고 중… 0.0초 · 2자` |
| AC2 | `show_thinking=false`에서 사고 내용 미노출 | **통과** | 진행 중 thinking 블록 `hidden=true`, 체크박스 `false` |
| AC3 | `show_thinking=true`에서 사고 내용 실시간 표시 | **통과** | 체크 후 `hidden=false`, 본문 `thinking We need answer in Korean likely…` |
| AC4 | 본문 도착 시 진행 표시 제거 후 답변 렌더 | **통과** | 본문 최초 표시 2,766ms, 그 시점 및 완료 후 `.thinking-progress` 없음 |
| AC5 | 완료 통계에 사고 토큰 표시 | **통과** | `3.71초 · 147자 · 40자/초 · 토큰 405 · 사고 243토큰` |
| AC6 | 기존 동작 회귀 없음 | **통과** | 프론트 13건, 백엔드 37건 전부 성공 |

## 개선 효과

| 지표 | 변경 전 | 변경 후 |
| --- | --- | --- |
| Enter 이후 화면에 첫 변화가 나타나기까지 | 12,622ms | **57ms** |
| 사고 진행 상황 파악 | 불가(정지와 구별 불가) | 경과 시간·누적 글자 수 표시 |
| 사고 토큰 소비량 확인 | 불가 | 완료 통계에 표시 |

## 실행한 검증

```sh
sh src/frontend/tests/smoke.sh                      # 13 pass
cd src/backend && python3 -m unittest discover -s tests -t .   # 37 pass
python3 scratchpad/cdp.py http://127.0.0.1:8080/    # 실제 브라우저 AC1~AC5
```

## 남은 위험 / 관찰

- 스트리밍 도중 전체 리렌더가 일어나면 진행 표시의 경과 시간이 0부터 다시 센다. 표시 전용 값이라 기능 영향은 없다(설계상 수용, EXECUTION 참고).
- 추론 파서가 없는 모델(`exaone-4.5-33b`)에서는 reasoning 이벤트가 없어 기존 안내 문구가 그대로 나온다. 실모델 확인은 하지 않았고 코드 경로상 기존 동작과 동일하다(SPEC R5).
- 사고 토큰 수는 vLLM이 보고할 때만 표시된다. `qwen3.8-27b`에서는 정상 보고를 확인했다.

## 판정

FEAT-002의 요구사항 R1~R4를 충족하고 회귀는 없다. R5는 코드 경로 기준으로만 확인했다. 병합 가능.
