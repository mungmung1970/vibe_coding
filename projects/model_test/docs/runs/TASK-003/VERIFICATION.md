# TASK-003 · VERIFICATION

검증일: 2026-09-07

## 인수 조건 결과

| # | 조건 | 결과 | 근거 |
| --- | --- | --- | --- |
| AC1 | SOT가 이 저장소를 정확히 기술 | **통과** | 기재한 Test 명령 2개를 실제 실행해 백엔드 37건·프론트 13건 성공 확인 |
| AC2 | SPEC이 구현과 일치 | **통과** | F1~F4를 API·UI 동작과 대조(아래 표) |
| AC3 | TASK-001 기록이 실제 결과 반영, 소급 명시 | **통과** | PLAN/EXECUTION/VERIFICATION에 소급 작성과 독립 검증 미충족을 명시 |
| AC4 | gpt-oss-120b 기동 또는 원인 문서화 | **통과** | 재시도 성공(751초), 실제 생성 확인 |
| AC5 | 콘솔이 정상 서빙 상태로 복귀 | **통과** | `gpt-oss-120b` ready 상태로 종료 |
| AC6 | 회귀 없음 | **통과** | 백엔드 37건, 프론트 13건 |

## gpt-oss-120b 기동 결과

| 항목 | 1차(실패) | 2차(성공) |
| --- | --- | --- |
| 가중치 로드 | 66.22GiB / 96.4초 | 66.22GiB / 41.6초 |
| torch.compile | 캐시 없음 | AOT 캐시 재사용 0.13초 |
| flashinfer cubin | 다운로드 중 프로세스 소멸 | 통과 |
| 준비 완료 | 실패(약 9분 후 오류) | **751초** |

1차 실패는 커널 캐시가 비어 있던 초기 상태에서 cubin을 내려받던 중 발생했고, 캐시가 채워진 2차에서는 재현되지 않았다. 코드 결함의 증거는 없다. 다시 발생하면 종료 코드가 함께 표시되므로 그때 판단한다(TASK-002 이후 반영).

### 실제 생성

| 요청 | 결과 |
| --- | --- |
| "LCA 리포트란 무엇인가요? 2문장" | 3,709ms / 129 완료토큰, 정확한 한국어 답변 |
| 합동식 문제(가장 작은 양의 정수) | 4.8~6.6초 / 1,500~1,700 완료토큰, 사고 스트림 분리 표시 |

## SPEC 대조 (AC2)

| SPEC | 확인 |
| --- | --- |
| F1.2 단일 서빙 | `qwen3.8-27b` → `gpt-oss-120b` 전환 시 이전 프로세스 종료 후 시작 확인 |
| F1.3 상태 전이 | `starting → ready` 및 실패 시 `error` + 종료 코드 표시 확인 |
| F2.2 전달 위치 | `reasoning_effort`가 `chat_template_kwargs`로 전달되어 템플릿에 반영됨을 vLLM `/tokenize`+`/detokenize`로 직접 확인: `Reasoning: low` / `Reasoning: high` |
| F2.5 검증 | 범위 초과·미지원 키가 422로 거부됨(테스트) |
| F3.2 스트리밍/사고 분리 | reasoning과 본문이 분리되어 각각 표시 |
| F4.3 비교 | `POST /runs/compare`가 파라미터 차이만 추출 |

> `reasoning_effort` 값에 따른 **출력 길이 차이**는 관찰되지 않았다(low 1,380자 / high 1,054자 사고). 전달 경로는 위와 같이 확인했으므로 콘솔 결함이 아니라 모델 편차로 판단한다.

## 종료 상태

- 서빙: `gpt-oss-120b` ready (재기동에 12분 이상 걸리는 모델이라 예열 상태를 유지한다. Qwen3.8은 약 3분이면 전환된다)
- 제품 소스 변경: 없음
- 문서 변경: SOT, SPEC, TASK-001 기록 3건, models/README, TASK-003 기록 3건

## 남은 사항

- 하니스 규약의 **독립 검증**은 여전히 미충족이다(구현자와 검증자가 동일). 별도 검증 담당이 필요하면 지정해 주어야 한다.
- `gemma-4-31b-it`, `exaone-4.5-33b`, `thinkingcap-qwen3.6-27b`는 아직 실제 서빙을 확인하지 않았다.
- `docs/adr/ADR-0001.md`, `docs/tdd/TEST_STRATEGY.md`, `docs/handoff/HANDOFF.md`는 여전히 비어 있다.
